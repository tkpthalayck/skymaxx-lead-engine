# SKYMAXX Lead Engine — Environment Variables
# Copy this to .env and fill in your values
# NEVER commit .env to Git

# Google Maps Places API Key
GOOGLE_MAPS_API_KEY=AIzaSyC_knxg1Z0ru5xfrLCQ8Rag1w8X1J0A_6Y

# ZeptoMail API Token (from ZeptoMail dashboard)
ZEPTO_TOKEN=Zoho-enczapikey your_token_here

# Verified sender email (must be verified in ZeptoMail)
FROM_EMAIL=ali@skymaxx.company

# Sender display name
FROM_NAME=Ali | SKYMAXX IT Solutions

# Database path (local dev only — Render uses /var/data/skymaxx.db)
DB_PATH=skymaxx.db
