name: LogoTest
on: { workflow_dispatch: , push: { paths: ['logo_test.py'] } }
permissions: { contents: write }
jobs:
  v:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - run: python3 logo_test.py
      - if: always()
        run: |
          git config user.email "b@s.c"
          git config user.name "B"
          git add -f logo_test.txt 2>/dev/null || true
          git diff --staged --quiet || git commit -m "logo test [skip ci]"
          git push || true
