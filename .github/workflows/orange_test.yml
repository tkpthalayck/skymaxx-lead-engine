name: OrangeTest
on: { workflow_dispatch: , push: { paths: ['orange_test.py'] } }
permissions: { contents: write }
jobs:
  v:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 orange_test.py
      - if: always()
        run: |
          git config user.email "b@s.c"
          git config user.name "B"
          git add -f orange_test.txt 2>/dev/null || true
          git diff --staged --quiet || git commit -m "test [skip ci]"
          git push || true
