name: OptionalTest
on: { workflow_dispatch: , push: { paths: ['optional_test.py'] } }
permissions: { contents: write }
jobs:
  v:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - run: python3 optional_test.py
      - if: always()
        run: |
          git config user.email "b@s.c"
          git config user.name "B"
          git add -f optional_test.txt 2>/dev/null || true
          git diff --staged --quiet || git commit -m "opt test [skip ci]"
          git push || true
