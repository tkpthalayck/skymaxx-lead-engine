name: AuditAll
on: { workflow_dispatch: , push: { paths: ['audit_all.py'] } }
permissions: { contents: write }
jobs:
  v:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - run: python3 audit_all.py
      - if: always()
        run: |
          git config user.email "b@s.c"
          git config user.name "B"
          git add -f audit.txt 2>/dev/null || true
          git diff --staged --quiet || git commit -m "v [skip ci]"
          git push || true
