name: Run Direct Edit Test
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sleep 10
      - run: python3 test_direct_edit.py
