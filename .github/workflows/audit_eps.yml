name: AuditEndpoints
on: { workflow_dispatch: , push: { paths: ['audit_eps.py'] } }
permissions: { contents: write }
jobs:
  v:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - run: python3 audit_eps.py
      - if: always()
        run: |
          git config user.email "b@s.c"
          git config user.name "B"
          git add -f audit.txt 2>/dev/null || true
          git diff --staged --quiet || git commit -m "audit [skip ci]"
          git push || true
