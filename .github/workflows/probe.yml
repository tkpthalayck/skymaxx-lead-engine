name: Probe
on: { workflow_dispatch: , push: { paths: ['probe.py'] } }
permissions: { contents: write }
jobs:
  v:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - run: python3 probe.py
      - if: always()
        run: |
          git config user.email "b@s.c"; git config user.name "B"
          git add -f probe.txt 2>/dev/null || true
          git diff --staged --quiet || git commit -m "p [skip ci]"
          git push || true
