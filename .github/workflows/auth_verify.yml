name: AuthVerify
on: { workflow_dispatch: , push: { paths: ['auth_verify.py'] } }
permissions: { contents: write }
jobs:
  v:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - run: python3 auth_verify.py
      - if: always()
        run: |
          git config user.email "b@s.c"; git config user.name "B"
          git add -f auth_verify.txt 2>/dev/null || true
          git diff --staged --quiet || git commit -m "av [skip ci]"
          git push || true
