name: FinalTest
on: { workflow_dispatch: , push: { paths: ['final_test.py'] } }
permissions: { contents: write }
jobs:
  v:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - run: python3 final_test.py
      - if: always()
        run: |
          git config user.email "b@s.c"
          git config user.name "B"
          git add -f final_test.txt 2>/dev/null || true
          git diff --staged --quiet || git commit -m "final [skip ci]"
          git push || true
