name: VFinal
on: { workflow_dispatch: , push: { paths: ['verify_final.py'] } }
permissions: { contents: write }
jobs:
  v:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - run: python3 verify_final.py
      - if: always()
        run: |
          git config user.email "b@s.c"; git config user.name "B"
          git add -f verify_final.txt 2>/dev/null || true
          git diff --staged --quiet || git commit -m "vf [skip ci]"
          git push || true
