name: VerifyCC
on: { workflow_dispatch: , push: { paths: ['verify_cc.py'] } }
permissions: { contents: write }
jobs:
  v:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - run: python3 verify_cc.py
      - if: always()
        run: |
          git config user.email "b@s.c"; git config user.name "B"
          git add -f verify_cc.txt 2>/dev/null || true
          git diff --staged --quiet || git commit -m "vcc [skip ci]"
          git push || true
